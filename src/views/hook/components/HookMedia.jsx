import React, { useState, useCallback, useEffect } from "react";
import styles from "../index.less";
import { Tag } from "antd";

const HookMedia = (props) => {
  function log(e) {
    console.log(e);
  }

  return <div className={styles.childWrap}></div>;
};

export default HookMedia;
